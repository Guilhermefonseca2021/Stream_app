import VideoProps from "../../types/video";

export default function Video({
  title,
  description,
  filename,
  videoUrl,
}: VideoProps) {
  return (
    <div>
      <p className="">Video</p>
    </div>
  );
}
